interface:
  display_name: "Peaks Route Catalog Audit"
  short_description: "Audit one peak route, name, and outside facts"
  default_prompt: "Use $peaks-route-catalog-audit to audit one queued peak against stored checks and two public sources."
